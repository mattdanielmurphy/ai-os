---
title: "[2026-06-22 14:22 MDT-6] and then angle is gotten with atan("
date: "2026-08-13"
source: "gemini.google.com"
conversation_id: "d9e294f9-be2f-5ba4-bdb0-f993ddc9d794"
url: ""
---

# [2026-06-22 14:22 MDT-6] and then angle is gotten with atan(

> [!NOTE]
> Archived Gemini Thread: [[2026-06-22 14:22 MDT-6] and then angle is gotten with atan(]() | Date: 2026-08-13

## User

[2026-06-22 14:22 MDT-6] and then angle is gotten with atan(p1'y/p1'x)?

---

## Gemini

import numpy as np

# Let's re-calculate to ensure the angle matches exactly.
# Initial momenta
# a1: m = 800, v = 320, angle = 30 (Q1)
p1x = 800 * 320 * np.cos(np.radians(30))
p1y = 800 * 320 * np.sin(np.radians(30))

# a2: m = 1250, v = 220, angle = 40 (Q2: North of West -> x is negative, y is positive)
p2x = -1250 * 220 * np.cos(np.radians(40))
p2y = 1250 * 220 * np.sin(np.radians(40))

# Final pieces of a2:
# a2': m = 700, v = 120, angle = 20 (Q1: North of East -> x is positive, y is positive)
p2prime_x = 700 * 120 * np.cos(np.radians(20))
p2prime_y = 700 * 120 * np.sin(np.radians(20))

# a3': m = (1250 - 700) = 550, v = 180, angle = 35 (Q3: South of West -> x is negative, y is negative)
p3prime_x = -550 * 180 * np.cos(np.radians(35))
p3prime_y = -550 * 180 * np.sin(np.radians(35))

# Total initial momentum
P_init_x = p1x + p2x
P_init_y = p1y + p2y

# Total final momentum excluding a1'
P_final_other_x = p2prime_x + p3prime_x
P_final_other_y = p2prime_y + p3prime_y

# a1' momentum
p1prime_x = P_init_x - P_final_other_x
p1prime_y = P_init_y - P_final_other_y

# Velocity
v1prime_x = p1prime_x / 800
v1prime_y = p1prime_y / 800
v1prime_mag = np.hypot(v1prime_x, v1prime_y)

angle_rad = np.arctan2(v1prime_y, v1prime_x)
angle_deg = np.degrees(angle_rad)

print(f"p1prime_x: {p1prime_x:.2f}, p1prime_y: {p1prime_y:.2f}")
print(f"v1prime_mag: {v1prime_mag:.2f}")
print(f"angle_deg: {angle_deg:.2f}")



p1prime_x: 13202.15, p1prime_y: 332820.97
v1prime_mag: 416.35
angle_deg: 87.73


Yes, exactly. Since both components ($p_{1x}'$ and $p_{1y}'$) are positive, the vector points into the first quadrant (East and North).


The angle $\theta$ relative to the positive $x$-axis (East) is found using:


$$\theta = \tan^{-1}\left(\frac{p_{1y}'}{p_{1x}'}\right)$$


Plugging in the corrected components:


$$\theta = \tan^{-1}\left(\frac{332,821\text{ N}\cdot\text{s}}{13,202\text{ N}\cdot\text{s}}\right) \approx 87.7^\circ \approx 88^\circ$$


This confirms the direction is $88^\circ\text{ North of East}$.

---

