---
title: "Refactor NDJSON Streaming Logic"
date: "2026-08-17"
conversation_id: "436c73d9-80bc-4a37-842c-c1bc4e1cc87a"
source: "antigravity"
---

<span style="display: flex; flex-direction: column-reverse; height: 100cqh; overflow-y: auto; overflow-x: hidden; box-sizing: border-box; width: 100cqw; max-width: 100cqw; min-width: 100%; position: absolute; top: 0; left: calc(50% - 50cqw - 2px); bottom: 0; padding: 2.5rem calc(2rem) 2.5rem calc(2rem + 2 * 2px); scrollbar-width: thin;">

<span style="display: block; text-align: center; opacity: 0.45; font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 1px; padding: 0 0 2.5rem 0;">Thread Started — August 17, 2026</span>

<span style="display: block; width: 100%; max-width: 100%; min-width: 0; box-sizing: border-box; margin-top: 8px; margin-bottom: 48px; overflow-wrap: anywhere; word-break: break-word;">

<span title="Sent at " style="display: block; width: fit-content; max-width: 80%; min-width: 0; margin-left: auto; box-sizing: border-box; overflow-wrap: anywhere; word-break: break-word; text-align: left; background: rgba(85, 68, 197, 0.16); border: 1.5px solid rgba(85, 68, 197, 0.45); padding: 10px 14px; border-radius: 14px 14px 2px 14px; white-space: pre-wrap; line-height: 1.45; font-size: 14px; margin-bottom: 16px; box-shadow: 0 2px 6px rgba(0,0,0,0.12);">Please make surgical edits to the following files:<br><br>1. Target File: /Users/matt/projects/yt-highlight-reel/api/generate.js<br><br>Replace lines 201 to 232:<br>```javascript<br>	const progress = new ProgressWriter(res)<br>	const logger = createGenerateLogger(progress, {<br>		mode: query ? 'multi' : 'single',<br>		videoId: videoId || null<br>	})<br><br>	const sendProgress = (p) => {<br>		progress.writeEvent('progress', p)<br>	}<br><br>	const clientIp = req.headers['x-forwarded-for'] || req.socket.remoteAddress || '127.0.0.1'<br><br>	try {<br>		logger.log('init', `Starting highlight reel generation`, { mode: query ? 'multi' : 'single', videoId, query })<br>		const rateCheck = await checkRateLimit(clientIp, 0.003)<br>		if (!rateCheck.allowed) {<br>			res.statusCode = 429<br>			res.setHeader('Content-Type', 'application/json')<br>			return res.end(JSON.stringify({<br>				error: 'rate_limited',<br>				reason: rateCheck.reason,<br>				message: rateCheck.reason === 'daily_cap'<br>					? 'Daily AI token budget limit reached. Please complete a quick 1-step survey to help us increase capacity.'<br>					: 'You have reached your 3 generations per day limit. Please complete our 1-step feedback survey.'<br>			}))<br>		}<br><br>		const apiKey = process.env.OPENROUTER_API_KEY<br>		if (!apiKey) {<br>			throw new Error('OpenRouter API key is not configured.')<br>		}<br>```<br><br>With:<br>```javascript<br>	const clientIp = req.headers['x-forwarded-for'] || req.socket.remoteAddress || '127.0.0.1'<br><br>	try {<br>		const rateCheck = await checkRateLimit(clientIp, 0.003)<br>		if (!rateCheck.allowed) {<br>			res.statusCode = 429<br>			res.setHeader('Content-Type', 'application/json')<br>			return res.end(JSON.stringify({<br>				error: 'rate_limited',<br>				reason: rateCheck.reason,<br>				message: rateCheck.reason === 'daily_cap'<br>					? 'Daily AI token budget limit reached. Please complete a quick 1-step survey to help us increase capacity.'<br>					: 'You have reached your 3 generations per day limit. Please complete our 1-step feedback survey.'<br>			}))<br>		}<br><br>		const apiKey = process.env.OPENROUTER_API_KEY<br>		if (!apiKey) {<br>			res.statusCode = 500<br>			res.setHeader('Content-Type', 'application/json')<br>			return res.end(JSON.stringify({ error: 'OpenRouter API key is not configured.' }))<br>		}<br><br>		res.setHeader('Content-Type', 'application/x-ndjson; charset=utf-8')<br>		res.setHeader('Cache-Control', 'no-cache, no-transform')<br>		res.setHeader('X-Content-Type-Options', 'nosniff')<br><br>		const progress = new ProgressWriter(res)<br>		const logger = createGenerateLogger(progress, {<br>			mode: query ? 'multi' : 'single',<br>			videoId: videoId || null<br>		})<br><br>		const sendProgress = (p) => {<br>			progress.writeEvent('progress', p)<br>		}<br><br>		logger.log('init', `Starting highlight reel generation`, { mode: query ? 'multi' : 'single', videoId, query })<br>```<br><br>2. Target File: /Users/matt/projects/yt-highlight-reel/src/ai.js<br><br>Replace lines 77 to 167:<br>```javascript<br>		// Handle streaming NDJSON response<br>		const contentType = res.headers.get("content-type") || ""<br>		if (contentType.includes("ndjson") && res.body) {<br>			const reader = res.body.getReader()<br>			const decoder = new TextDecoder()<br>			let buffer = ""<br>			let finalReel = null<br><br>			while (true) {<br>				const { done, value } = await reader.read()<br>				if (done) break<br><br>				lastEventAt = Date.now()<br>				buffer += decoder.decode(value, { stream: true })<br>				const lines = buffer.split("\n")<br>				buffer = lines.pop() // keep unparsed remainder<br><br>				for (const line of lines) {<br>					const trimmed = line.trim()<br>					if (!trimmed) continue<br>					let ev = null<br>					try {<br>						ev = JSON.parse(trimmed)<br>					} catch (e) {<br>						continue<br>					}<br><br>					if (ev.type === 'log') {<br>						recentLogBuffer.push(ev)<br>						if (recentLogBuffer.length > 50) recentLogBuffer.shift()<br>						console.info(`[AI Log ${ev.ts?.slice(11, 19) || ''}] [${ev.stage || ''}] ${ev.detail || ''}`)<br>					}<br><br>					if (ev.type === 'error' || ev.stage === 'error' || ev.error) {<br>						throw new AiHighlightError(ev.message || ev.error || 'AI Generation failed', {<br>							code: ev.error || 'generation_failed',<br>							requestId: ev.requestId,<br>							stage: ev.stage,<br>							ts: ev.ts,<br>							diagnostics: ev.diagnostics<br>						})<br>					}<br>					<br>					if (ev.stage === 'done' && ev.reel) {<br>						finalReel = ev.reel<br>					} else if (ev.segments) {<br>						finalReel = ev<br>					} else if (ev.type === 'heartbeat' || ev.type === 'edit' || ev.detail || ev.pct) {<br>						emitProgress({ ...ev, mode })<br>					}<br>				}<br>			}<br><br>			// Process any remaining chunk in buffer<br>			if (buffer && buffer.trim()) {<br>				try {<br>					const ev = JSON.parse(buffer.trim())<br>					if (ev.type === 'error' || ev.stage === "error" || ev.error) {<br>						throw new AiHighlightError(ev.message || ev.error || "AI Generation failed", {<br>							requestId: ev.requestId,<br>							stage: ev.stage,<br>							ts: ev.ts<br>						})<br>					}<br>					if (ev.stage === "done" && ev.reel) {<br>						finalReel = ev.reel<br>					} else if (ev.segments) {<br>						finalReel = ev<br>					}<br>				} catch (e) {<br>					if (e instanceof AiHighlightError) throw e<br>				}<br>			}<br><br>			if (finalReel && finalReel.segments && finalReel.segments.length > 0) {<br>				emitProgress({ stage: "done", detail: "Highlight reel ready!", pct: 100, step: 3, totalSteps: 3, mode })<br>				return finalReel<br>			}<br><br>			if (finalReel && (!finalReel.segments || finalReel.segments.length === 0)) {<br>				throw new AiHighlightError(finalReel.fallbackReason || 'Highlight reel generation finished but returned no clips.')<br>			}<br><br>			throw new AiHighlightError('Highlight reel stream ended without completing clips.')<br>		}<br><br>		// Fallback for standard non-streaming JSON responses<br>		const data = await res.json()<br>		if (data.reel) return data.reel<br>		return data<br>```<br><br>With:<br>```javascript<br>		// Read response body as stream or text to support both NDJSON and standard JSON cleanly<br>		let finalReel = null<br><br>		if (res.body && typeof res.body.getReader === 'function') {<br>			const reader = res.body.getReader()<br>			const decoder = new TextDecoder()<br>			let buffer = ""<br><br>			while (true) {<br>				const { done, value } = await reader.read()<br>				if (done) break<br><br>				lastEventAt = Date.now()<br>				buffer += decoder.decode(value, { stream: true })<br>				const lines = buffer.split("\n")<br>				buffer = lines.pop() // keep unparsed remainder<br><br>				for (const line of lines) {<br>					const trimmed = line.trim()<br>					if (!trimmed) continue<br>					let ev = null<br>					try {<br>						ev = JSON.parse(trimmed)<br>					} catch (e) {<br>						continue<br>					}<br><br>					if (ev.type === 'log') {<br>						recentLogBuffer.push(ev)<br>						if (recentLogBuffer.length > 50) recentLogBuffer.shift()<br>						console.info(`[AI Log ${ev.ts?.slice(11, 19) || ''}] [${ev.stage || ''}] ${ev.detail || ''}`)<br>					}<br><br>					if (ev.type === 'error' || ev.stage === 'error' || ev.error) {<br>						throw new AiHighlightError(ev.message || ev.error || 'AI Generation failed', {<br>							code: ev.error || 'generation_failed',<br>							requestId: ev.requestId,<br>							stage: ev.stage,<br>							ts: ev.ts,<br>							diagnostics: ev.diagnostics<br>						})<br>					}<br>					<br>					if (ev.stage === 'done' && ev.reel) {<br>						finalReel = ev.reel<br>					} else if (ev.reel && ev.reel.segments) {<br>						finalReel = ev.reel<br>					} else if (ev.segments) {<br>						finalReel = ev<br>					} else if (ev.type === 'heartbeat' || ev.type === 'edit' || ev.detail || ev.pct) {<br>						emitProgress({ ...ev, mode })<br>					}<br>				}<br>			}<br><br>			// Process any remaining chunk in buffer<br>			if (buffer && buffer.trim()) {<br>				try {<br>					const ev = JSON.parse(buffer.trim())<br>					if (ev.type === 'error' || ev.stage === "error" || ev.error) {<br>						throw new AiHighlightError(ev.message || ev.error || "AI Generation failed", {<br>							requestId: ev.requestId,<br>							stage: ev.stage,<br>							ts: ev.ts<br>						})<br>					}<br>					if (ev.stage === "done" && ev.reel) {<br>						finalReel = ev.reel<br>					} else if (ev.reel && ev.reel.segments) {<br>						finalReel = ev.reel<br>					} else if (ev.segments) {<br>						finalReel = ev<br>					}<br>				} catch (e) {<br>					if (e instanceof AiHighlightError) throw e<br>				}<br>			}<br>		} else {<br>			const text = await res.text()<br>			const lines = text.split("\n")<br>			for (const line of lines) {<br>				const trimmed = line.trim()<br>				if (!trimmed) continue<br>				try {<br>					const ev = JSON.parse(trimmed)<br>					if (ev.stage === 'done' && ev.reel) finalReel = ev.reel<br>					else if (ev.reel && ev.reel.segments) finalReel = ev.reel<br>					else if (ev.segments) finalReel = ev<br>				} catch (e) {}<br>			}<br>		}<br><br>		if (finalReel && finalReel.segments && finalReel.segments.length > 0) {<br>			emitProgress({ stage: "done", detail: "Highlight reel ready!", pct: 100, step: 3, totalSteps: 3, mode })<br>			return finalReel<br>		}<br><br>		if (finalReel && (!finalReel.segments || finalReel.segments.length === 0)) {<br>			throw new AiHighlightError(finalReel.fallbackReason || 'Highlight reel generation finished but returned no clips.')<br>		}<br><br>		throw new AiHighlightError('Highlight reel stream ended without completing clips.')<br>```<br><br>3. Target File: /Users/matt/projects/yt-highlight-reel/test/ai.test.js<br><br>Add a test at the end of the file:<br>```javascript<br>test("NDJSON Stream Parser handles stream without explicit ndjson content-type header", async () => {<br>	const mockChunks = [<br>		JSON.stringify({ type: 'log', stage: "init", detail: "Starting generation" }) + "\n",<br>		JSON.stringify({ type: 'progress', stage: "fetch", pct: 10, detail: "Fetching transcript" }) + "\n",<br>		JSON.stringify({ stage: "done", pct: 100, reel: { type: "single", videoId: "TEST9999", segments: [{ start: 5, end: 15, title: "Test Clip" }] } }) + "\n"<br>	]<br><br>	const originalFetch = global.fetch<br>	global.fetch = async () => {<br>		const stream = new ReadableStream({<br>			start(controller) {<br>				for (const chunk of mockChunks) {<br>					controller.enqueue(new TextEncoder().encode(chunk))<br>				}<br>				controller.close()<br>			}<br>		})<br>		// Simulating generic application/json header from proxy or server<br>		return new Response(stream, {<br>			headers: { "Content-Type": "application/json" },<br>			status: 200<br>		})<br>	}<br><br>	const { requestAiHighlights } = await import("../src/ai.js")<br>	const result = await requestAiHighlights({ videoId: "TEST9999" })<br>	global.fetch = originalFetch<br><br>	assert.ok(result)<br>	assert.equal(result.videoId, "TEST9999")<br>	assert.equal(result.segments.length, 1)<br>})<br>```</span>



<span title="Responded at 7:17am" style="display: block; width: 100%; max-width: 100%; min-width: 0; box-sizing: border-box; overflow-wrap: anywhere; word-break: break-word; text-align: left; background: none; border: 1.5px solid rgba(113, 100, 175, 0.35); padding: 16px 20px; border-radius: 14px 14px 14px 2px; line-height: 1.6; font-size: 14px; margin-bottom: 24px; box-shadow: 0 2px 6px rgba(0,0,0,0.08);">

The requested surgical edits have been applied to `generate.js`, `ai.js`, and `ai.test.js`.

### Summary of Changes

*   **[`generate.js`](file:///Users/matt/projects/yt-highlight-reel/api/generate.js) [](http://127.0.0.1:8643/open_zed?path=/Users/matt/projects/yt-highlight-reel/api/generate.js) [](http://127.0.0.1:8643/open_finder?path=/Users/matt/projects/yt-highlight-reel/api/generate.js)**:
    *   Moved the initialization of `ProgressWriter` and `createGenerateLogger` *after* the header configuration and API key check to ensure the response stream is correctly configured before any progress events are emitted.
    *   Added explicit `Content-Type: application/x-ndjson` and cache control headers.
*   **[`ai.js`](file:///Users/matt/projects/yt-highlight-reel/src/ai.js) [](http://127.0.0.1:8643/open_zed?path=/Users/matt/projects/yt-highlight-reel/src/ai.js) [](http://127.0.0.1:8643/open_finder?path=/Users/matt/projects/yt-highlight-reel/src/ai.js)**:
    *   Unified the logic for consuming stream/text responses, ensuring the parser handles both streaming and buffered data. This allows the parser to process NDJSON lines correctly regardless of whether the `Content-Type` header is strictly set to `application/x-ndjson`.
*   **[`ai.test.js`](file:///Users/matt/projects/yt-highlight-reel/test/ai.test.js) [](http://127.0.0.1:8643/open_zed?path=/Users/matt/projects/yt-highlight-reel/test/ai.test.js) [](http://127.0.0.1:8643/open_finder?path=/Users/matt/projects/yt-highlight-reel/test/ai.test.js)**:
    *   Added a regression test that verifies the parser's ability to handle NDJSON stream chunks when receiving a generic `application/json` header, confirming the fix.

Please refer to the updated files for the detailed implementation.

</span>



</span>

<span style="position: absolute; left: 0; right: 0; bottom: 0; width: 100cqw; padding: 0 2rem;">

| Tokens | Expiry | Committed | PPLX Quota |
| :--- | :--- | :--- | :--- |
| ~40k / ~430k 🟢 (optimal) | 2:15am | 🟡 Uncommitted (1) | 96 ❓, 30 📤 |

<span style="position: absolute; right: 2rem; bottom: 0.5rem; display: inline-block; font-size: 11px; font-weight: 600; opacity: 0.7; padding: 3px 10px; border: 1px solid rgba(113,100,175,0.4); border-radius: 20px; white-space: nowrap; letter-spacing: 0.3px;"><a href="file:///Users/matt/.gemini/antigravity/brain/436c73d9-80bc-4a37-842c-c1bc4e1cc87a/kanban.md" style="text-decoration:none;">📋 Kanban</a></span>

</span>

</span>