---
title: "Test ask_perplexity with Sonnet"
date: "2026-08-11"
conversation_id: "1ac94292b6da"
source: "antigravity"
---

# Test ask_perplexity with Sonnet

## User

try a test with ask_perplexity, use sonnet

---

## Assistant

I'll try asking the user-provided `ask_perplexity` tool with the sonnet model. Let me check what's available first.

No `ask_perplexity` tool matched. Let me check the connected sources — the only available tool source is `agymcp`. Let me search more broadly to confirm whether anything like this exists.

There's no `ask_perplexity` tool available in this environment. The only connected tool source is `agymcp` (agy CLI tools), and none of them match Perplexity.

Let me check if `ask_perplexity` is a CLI binary or script on the system instead.

There's no `ask_perplexity` CLI on the system — the only Perplexity artifacts are app-related plist/cache files and a Raycast extension script. No command-line Perplexity tool is installed.

Let me also check the project directory for any custom scripts that might define `ask_perplexity`, in case it's project-local.

---
