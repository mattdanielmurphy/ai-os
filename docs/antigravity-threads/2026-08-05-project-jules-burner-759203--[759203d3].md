---
title: "Project: jules-burner [759203]"
date: "2026-08-05"
conversation_id: "759203d3-fe59-46b5-b12e-ef79973ecd54"
source: "antigravity"
---

# Project: jules-burner [759203]

## User

Create the following files under /Users/matt/projects/jules-burner (ensure directories are created, use write_to_file tool with Overwrite=true):

1. `package.json`
```json
{
  "name": "jules-burner",
  "version": "1.0.0",
  "description": "Jules Quota Burner",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0"
  }
}
```

2. `tsconfig.json`
```json
{
  "compilerOptions": {
    "lib": ["ESNext"],
    "module": "esnext",
    "target": "esnext",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "composite": true,
    "strict": true,
    "downlevelIteration": true,
    "skipLibCheck": true,
    "jsx": "react-jsx",
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,
    "allowJs": true,
    "types": [
      "bun-types"
    ]
  }
}
```

3. `src/types.ts`
```typescript
export interface Task {
  id: string;
  prompt: string;
  targetRepository?: string;
  status: 'PENDING' | 'DISPATCHED' | 'FAILED' | 'COMPLETED';
}
```

4. `src/config.ts`
```typescript
import { z } from "zod";

const envSchema = z.object({
  GITHUB_TOKEN: z.string().optional(),
  JULES_CLI_PATH: z.string().default("jules"),
  DATABASE_PATH: z.string().default("data/audit.db"),
});

const env = envSchema.parse(process.env);

export const config = {
  DAILY_TASK_LIMIT: 100,
  MIN_JITTER_SECONDS: 180,
  MAX_JITTER_SECONDS: 420,
  STAGING_ORG_OR_USER: "staging-org",
  GITHUB_TOKEN: env.GITHUB_TOKEN,
  JULES_CLI_PATH: env.JULES_CLI_PATH,
  DATABASE_PATH: env.DATABASE_PATH,
};
```

5. `src/safety/firewall.ts`
```typescript
import { config } from "../config";
import type { Task } from "../types";

export class Firewall {
  private dailyTaskCount: number;

  constructor(initialCount = 0) {
    this.dailyTaskCount = initialCount;
  }

  public getDailyTaskCount(): number {

<truncated 1041 bytes>
all } from "../../src/safety/firewall";
import { config } from "../../src/config";
import type { Task } from "../../src/types";

describe("Safety Firewall", () => {
  let firewall: Firewall;

  beforeEach(() => {
    firewall = new Firewall();
  });

  test("allows safe tasks", () => {
    const task: Task = {
      id: "1",
      prompt: "Add a test file",
      targetRepository: "staging-jules-burner",
      status: "PENDING"
    };
    const result = firewall.checkDispatchSafe(task);
    expect(result.safe).toBe(true);
  });

  test("blocks tasks exceeding 100/day limit", () => {
    firewall = new Firewall(config.DAILY_TASK_LIMIT);
    const task: Task = {
      id: "2",
      prompt: "Safe prompt",
      targetRepository: "staging-app",
      status: "PENDING"
    };
    const result = firewall.checkDispatchSafe(task);
    expect(result.safe).toBe(false);
    expect(result.reason).toContain("Account limit");
  });

  test("blocks tasks with illegal prompt patterns", () => {
    const task: Task = {
      id: "3",
      prompt: "Write a scraper for google.com",
      targetRepository: "staging-app",
      status: "PENDING"
    };
    const result = firewall.checkDispatchSafe(task);
    expect(result.safe).toBe(false);
    expect(result.reason).toContain("illegal prompt pattern");
  });
  
  test("blocks non-staging repositories", () => {
    const task: Task = {
      id: "4",
      prompt: "Safe prompt",
      targetRepository: "prod-app",
      status: "PENDING"
    };
    const result = firewall.checkDispatchSafe(task);
    expect(result.safe).toBe(false);
    expect(result.reason).toContain("Fork Isolation Guard");
  });
});
```

Create all these files. Do NOT use multi_replace_file_content or replace_file_content unless the file exists, but these should be new files, so use write_to_file. Do not include markdown codeblock tags around your content when writing to files. Let me know when done.

---

## Assistant

Created At: 2026-08-05T00:51:17-06:00
Completed At: 2026-08-05T00:51:17-06:00
Created file file:///Users/matt/projects/jules-burner/package.json with requested content.
If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.

Created At: 2026-08-05T00:51:18-06:00
Completed At: 2026-08-05T00:51:19-06:00
Created file file:///Users/matt/projects/jules-burner/tsconfig.json with requested content.
If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.

Created At: 2026-08-05T00:51:20-06:00
Completed At: 2026-08-05T00:51:20-06:00
Created file file:///Users/matt/projects/jules-burner/src/types.ts with requested content.
If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.

Created At: 2026-08-05T00:51:21-06:00
Completed At: 2026-08-05T00:51:21-06:00
Created file file:///Users/matt/projects/jules-burner/src/config.ts with requested content.
If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.

Created At: 2026-08-05T00:51:23-06:00
Completed At: 2026-08-05T00:51:23-06:00
Created file file:///Users/matt/projects/jules-burner/src/safety/firewall.ts with requested content.
If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.

Created At: 2026-08-05T00:51:25-06:00
Completed At: 2026-08-05T00:51:25-06:00
Created file file:///Users/matt/projects/jules-burner/tests/safety/firewall.test.ts with requested content.
If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.

Created At: 2026-08-05T00:51:26-06:00
Completed At: 2026-08-05T00:51:26-06:00
Message sent to "890096da-458d-400d-ae4d-ebd8c0a7c9dd".

All files have been successfully created.

---
