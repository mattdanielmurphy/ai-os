---
title: "Create the implementation plan artifact at `/Users/matt/.gemini/antigravity/brain/4fa51f19-6ff3-4d2c-8162-2c5dcb529520/implementation_plan.md` using write_to_file with ArtifactMetadata (RequestFeedback: true, UserFacing: true, Summary: "Implementation plan to tear out mock data, integrate live GitHub bounty search, lock pipeline to a single end-to-end Jules bounty test, and verify automated execution.") and the following content:"
date: "2026-08-06"
conversation_id: "bf485764-9de7-4aec-94d2-45be65a8b22a"
source: "antigravity"
---

# Create the implementation plan artifact at `/Users/matt/.gemini/antigravity/brain/4fa51f19-6ff3-4d2c-8162-2c5dcb529520/implementation_plan.md` using write_to_file with ArtifactMetadata (RequestFeedback: true, UserFacing: true, Summary: "Implementation plan to tear out mock data, integrate live GitHub bounty search, lock pipeline to a single end-to-end Jules bounty test, and verify automated execution.") and the following content:

## User

Create the implementation plan artifact at `/Users/matt/.gemini/antigravity/brain/4fa51f19-6ff3-4d2c-8162-2c5dcb529520/implementation_plan.md` using write_to_file with ArtifactMetadata (RequestFeedback: true, UserFacing: true, Summary: "Implementation plan to tear out mock data, integrate live GitHub bounty search, lock pipeline to a single end-to-end Jules bounty test, and verify automated execution.") and the following content:

```markdown
# Live End-to-End Bounty PR Execution Plan (Jules)

Tear out all mock candidate data across discovery pipelines, restrict the system exclusively to live GitHub/Algora bounty candidates (focusing on low-complexity, easy-win bounties), and execute a single end-to-end test where Jules mines a real bounty, works on a fork, passes validation, and submits a real PR.

## User Review Required

> [!IMPORTANT]
> **Single Task Lock**: All internal self-tasks (\`SelfTasksPipeline\` and \`AssetSpecsPipeline\`) will be disabled. The system will ONLY execute live bounty candidates until the end-to-end Jules bounty flow is fully verified.

> [!NOTE]
> **GitHub Authentication**: The pipeline will use \`GITHUB_TOKEN\` from \`.env\` to query live issues via GitHub Search API (filtering for bounty tags, \`good-first-issue\`, \`help-wanted\`, or Algora bounties) and submit pull requests.

## Open Questions

- None at this time. The priority is to validate a live bounty PR end-to-end with minimum friction.

## Proposed Changes

### Discovery Module

#### [MODIFY] [micro_bounties.ts](file:///Users/matt/projects/jules-burner/src/discovery/pipelines/micro_bounties.ts)
- Tear out mock data.
- Implement live fetching using GitHub Search API / Algora endpoints to find open micro-bounties and \`good-first-issue\` items.

#### [MODIFY] [self_tasks.ts](file:///Users/matt/projects/jules-burner/src/discovery/pipelines/self_tasks.ts)
- Disable/empty internal self-tasks so zero internal tasks pollute the queue.

#### [MODIFY] [asset_specs.ts](file:///Users/matt/projects/jules-burner/src/discovery/pipelines/asset_specs.ts)
- Disable/empty internal asset spec tasks.

#### [MODIFY] [ranker.ts](file:///Users/matt/projects/jules-burner/src/discovery/ranker.ts)
- Update ranking algorithm to heavily prioritize low-complexity, low-risk, easy-win bounties ("cheaper/easier the better").

---

### Daemon & Staging

#### [MODIFY] [dispatcher.ts](file:///Users/matt/projects/jules-burner/src/daemon/dispatcher.ts)
- Ensure target repository is set to the actual external target repo (or staging fork \`mattdanielmurphy/<repo>\`), not hardcoded \`jules-burner\`.
- Support single-task test execution mode (\`--single-bounty-test\`).

#### [MODIFY] [auto_pr.ts](file:///Users/matt/projects/jules-burner/src/staging/auto_pr.ts)
- Replace hardcoded \`mattdanielmurphy/jules-burner\` PR target with the candidate's actual upstream/fork repository.

---

### End-to-End Test Suite

#### [NEW] [test_live_bounty.ts](file:///Users/matt/projects/jules-burner/tests/test_live_bounty.ts)
- Automated test script that:
  1. Mines a live low-complexity bounty via \`MicroBountiesPipeline\`.
  2. Spawns Jules session via Jules REST API / CLI (\`jules remote new --repo ...\`).
  3. Waits for session completion, pulls diff, runs sanity checks.
  4. Triggers \`UpstreamPRGate\` to publish the PR.

## Verification Plan

### Automated Tests
- Run \`bun test tests/test_live_bounty.ts\` to execute the full end-to-end live bounty workflow.
- Run \`bun test\` to ensure all unit tests remain green.

### Manual Verification
- Check GitHub PR URL generated by Jules on the target repository.
```

---
