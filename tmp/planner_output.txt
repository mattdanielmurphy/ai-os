## Summary

I've delivered the full Stage 3 plan as a Markdown file, grounded in the actual `mattdanielmurphy/ai-os` repo structure I inspected (`apps/gemini-companion/src-tauri/src/{main,server,session,threads,types}.rs`, the frontend TS files, `userscripts/`, and the currently-empty `context/threads/` tree — it doesn't exist yet, only `context/clinical-profile.md` is present).

Key architectural call: rather than reverse-engineering Gemini's `batchexecute` protocol or Perplexity's private `/rest/*` auth in Rust (fragile), the plan extends your existing **Context Sync Protocol** pattern — two new **hidden Tauri webviews** (`gemini-sync`, `perplexity-sync`) sharing the authenticated session, running injected JS listers/fetchers that POST results back to new `/api/cloud-sync/*` routes on the existing loopback daemon. Diffing against locally-known threads happens in Rust via a new `cloud_sync.rs` module, and completed thread turn-histories are atomically written to `context/threads/<provider>/<remote_id>-<slug>.md`.

The file covers all six required sections in depth — data structures, exact Rust/TS function signatures, step-by-step polling/ingest pseudocode, a full error-handling table (auth loss, rate limits, partial fetches, ID collisions between mobile-originated and desktop-continued threads, etc.), and a 13-step ordered implementation checklist across both the `ai-os` repo and the separate `userscript-bundler` repo.

One caveat worth flagging: the GitHub connector in this session returned file *existence/metadata* but not raw text bodies for `threads.rs`/`server.rs`/`main.ts`, so I couldn't confirm the exact existing struct/function names in `threads.rs` for local-thread cross-referencing, or Gemini/Perplexity's precise internal listing endpoints. I called these out explicitly as verification steps in the plan rather than guessing, so the implementing agent has zero ambiguity about what still needs a quick look at the live file/DevTools before coding.