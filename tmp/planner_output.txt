I've put together the full plan as a markdown file. Key points to flag before your downstream agent starts implementing:

## What I could and couldn't verify

I confirmed the exact file inventory, sizes, and change history for `apps/gemini-companion/src-tauri` directly from the repo  — notably that `shell_bridge.rs`, `context_snapshot.rs`, and `fs_bridge.rs` were all added together in commit `da914ab`, and `proxy.rs` had 45 lines stripped in that same commit (likely where the missing `start_proxy_server` export disappeared). The connector wouldn't surface raw file text in this session, so exact current line-by-line contents weren't available — the plan flags every spot where the implementing agent must open the file first rather than guess.

## Core fixes in the plan

- **Cargo.toml**: add `sha2 = "0.10"`, remove any leftover `wait_timeout` entry, and check for version conflicts with anything already in `Cargo.lock`.
- **shell_bridge.rs**: replace the sync `wait_timeout` pattern with `tokio::process::Command` + `tokio::time::timeout`, introducing typed `ShellCommandSpec`/`ShellCommandResult`/`ShellBridgeError` structs and a single consistent UTF-8 decode path (`from_utf8_lossy` for both stdout/stderr) to kill the byte-slice mismatch.
- **proxy.rs**: a concrete `async fn start_proxy_server(app_handle: tauri::AppHandle) -> Result<(), ProxyServerError>` that binds a listener, spawns the accept loop, and registers state via `app_handle.manage(...)` — reusing whatever HTTP stack `server.rs` already has rather than pulling in a second framework.
- **Lint cleanup**: a mechanical `cargo check`-driven pass across the four flagged files, with guidance on when to delete vs. `_`-prefix vs. actually wire up "unused" code.

## Notable edge cases called out

- Killing the child process explicitly on timeout (Tokio doesn't always do this on drop).
- Propagating the new `async` shell-bridge API up through any existing sync callers in `main.rs`/`pty.rs`/`session.rs`.
- Handling `AddrInUse` on proxy bind without crashing the whole Tauri app.

The file is ready for your implementing agent — it's structural/contract-level only, no full code, per your instructions.